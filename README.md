# proteinEcommerceFront

Storefront for Protein Island — lab-tested whey, gainers and creatine.

```bash
npm install
npm run dev
```

Runs on http://localhost:3000 and proxies `/api` to the backend on port 4000.
